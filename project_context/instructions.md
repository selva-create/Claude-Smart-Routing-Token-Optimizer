# Project Instructions

Replace this file with the standing instructions for your own project. It is
read on every turn and sits in the cached portion of the system prompt, so
keep it stable — editing it invalidates the cache prefix for the next call.

Default placeholder content:

- Role: Principal engineer assisting on this repository.
- Answer in code first. Explanation only when the code needs it.
- Match the existing code style in codebase_summary.md rather than
  introducing a new pattern.
- Flag any request that would require a dependency not already listed in
  package.json before adding one.
